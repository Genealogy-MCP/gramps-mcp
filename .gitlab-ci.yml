include:
  - project: facastagnini/ci-templates
    ref: main
    file: /pipelines/default.yml

variables:
  ENABLE_YAMLLINT: "false"
  ENABLE_TEST: "false"
  ENABLE_TEST_MATRIX: "false"
  UV_SYNC_ARGS: ""
  LINT_PATHS: "src"
  HEADER_PATHS: "src/ scripts/"
  YAMLLINT_EXCLUDE: ".github grampsweb-docs"

# gramps-mcp requires Docker Compose integration tests against a
# Gramps Web API instance. These cannot use the shared test jobs.

.test-base:
  stage: test
  retry:
    max: 2
    when:
      - script_failure
  services:
    - name: docker:dind
      command: ["--tls=false"]
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  before_script:
    - apt-get update && apt-get install -y --no-install-recommends ca-certificates curl gnupg
    - install -m 0755 -d /etc/apt/keyrings
    - curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
    - echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian bookworm stable" > /etc/apt/sources.list.d/docker.list
    - apt-get update && apt-get install -y --no-install-recommends docker-ce-cli docker-compose-plugin
    - pip install uv
    - docker compose -f docker-compose.test.yml pull
  script:
    - docker compose -f docker-compose.test.yml up -d --wait
    - |
      DOCKER_IP=$(getent ahostsv4 docker | head -1 | awk '{print $1}')
      PYTHON_IMG="python:$(python3 -c 'import sys; print(f"{sys.version_info.major}.{sys.version_info.minor}")')-slim-bookworm"
      docker run --rm \
        --network=host \
        -v "$CI_PROJECT_DIR:/app" \
        -w /app \
        -e REQUIRE_INTEGRATION=1 \
        -e GRAMPS_API_URL=http://127.0.0.1:5055 \
        -e GRAMPS_USERNAME=owner \
        -e GRAMPS_PASSWORD=owner \
        -e DOCKER_HOST="tcp://${DOCKER_IP}:2375" \
        -e COMPOSE_PROJECT_NAME=gramps-mcp \
        "$PYTHON_IMG" \
        bash -c '
          set -ex &&
          apt-get update &&
          apt-get install -y --no-install-recommends ca-certificates curl gnupg &&
          install -m 0755 -d /etc/apt/keyrings &&
          curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg &&
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian bookworm stable" > /etc/apt/sources.list.d/docker.list &&
          apt-get update &&
          apt-get install -y --no-install-recommends docker-ce-cli docker-compose-plugin &&
          docker version &&
          pip install uv &&
          uv sync &&
          uv run python scripts/seed_test_db.py &&
          uv run pytest
        '
  after_script:
    - docker compose -f docker-compose.test.yml logs --tail=100 || true
    - docker compose -f docker-compose.test.yml down -v || true

test-py3.10:
  extends:
    - .only-mr
    - .test-base
  image: python:3.10-slim-bookworm

test-py3.11:
  extends:
    - .only-mr
    - .test-base
  image: python:3.11-slim-bookworm

test-py3.12:
  extends:
    - .only-mr
    - .test-base
  image: python:3.12-slim-bookworm

test-py3.13:
  extends:
    - .only-mr
    - .test-base
  image: python:3.13-slim-bookworm
